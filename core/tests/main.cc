// doctest's runner lives in exactly one translation unit; the rest of the
// suite includes the header without this define and links against it.
#define DOCTEST_CONFIG_IMPLEMENT_WITH_MAIN
#include "doctest.h"
